cmake_minimum_required(VERSION 2.8.12)

project(SIFT)

find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})

add_executable(SIFT demo.cpp)
target_link_libraries(SIFT ${OpenCV_LIBS})
